#!/usr/bin/python3
"""Defines a matrix multiplicaion function using NumPy."""
import numpy as np


def matrix_mulltiplication(m_a, m_b):
    """Return the multiplication of two matrices."""

    return (np.matmul(m_a, m_b))
